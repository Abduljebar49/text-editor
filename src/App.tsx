import { TextEditor } from ".";

function App() {
  return (
    <main>
      <TextEditor
        // onChange={(content, html, title) => {
        // }}
      />
    </main>
  );
}

export default App;
